import StaffPage from '../../components/StaffPage';
import { WAITERS } from '../../constants/staff.constant';

const Waiters = () => {
  return <StaffPage staffType={WAITERS} />;
};

export default Waiters;
